{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "UnitSystem": {
      "description": "Unit basis curve power values are stored in. COMPONENT_BASE: per-unit on the component's own base_power. NATURAL_UNITS: MW/MVA. There is no system-base option: per-unit data historically on the system base records that base in the component's base_power and rides as COMPONENT_BASE.",
      "enum": [
        "COMPONENT_BASE",
        "NATURAL_UNITS"
      ],
      "title": "UnitSystem",
      "type": "string"
    }
  },
  "id": "#root"
}
